name: fisiocare
description: FisioCare - Homecare Fisioterapi App

publish_to: 'none'

version: 1.0.0+1
# The following defines the version and build number for your application.
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  intl: ^0.18.1
  table_calendar: ^3.0.9
  flutter_svg: ^2.0.9
  http: ^1.2.1
  supabase_flutter: ^2.5.6
  image_picker: ^0.8.7+4
  geolocator: ^10.1.0
  file_picker: ^8.0.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.jpeg
